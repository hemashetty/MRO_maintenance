<openerp>
  <data>

    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="mro_module.list">
      <field name="name">mro_module list</field>
      <field name="model">mro_module.mro_module</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="mro_module.action_window">
      <field name="name">mro_module window</field>
      <field name="res_model">mro_module.mro_module</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="mro_module.action_server">
      <field name="name">mro_module server</field>
      <field name="model_id" ref="model_mro_module_mro_module"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="mro_module" id="mro_module.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="mro_module.menu_1" parent="mro_module.menu_root"/>
    <menuitem name="Menu 2" id="mro_module.menu_2" parent="mro_module.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="mro_module.menu_1_list" parent="mro_module.menu_1"
              action="mro_module.action_window"/>
    <menuitem name="Server to list" id="mro_module" parent="mro_module.menu_2"
              action="mro_module.action_server"/>
    -->
  </data>
</openerp>